# intermediate build products for Windows and Linux
*.o
*.a

# the project for MacOSX
*.xcodeproj/

# intermediate build products for MacOSX
DerivedData

# OSX-specific files
.DS_Store

# Windows-specific files
Thumbs.db

# editor temporaries
*.kate-swp
*.orig
.cache/
.idea/
compile_commands.json

# Praat temporary files
*kanweg*

# a file that everybody should copy explicitly from their makefiles directory
# and then rename
makefile.defs

# executables
praat
Praat
Praat.exe
Praat.app

# files created by R scripts in the test folders
.Rdata
.Rhistory
.Rproj.user

# iOS port: linked binaries without an extension (object files/libs already ignored above)
ios/praat_barren_ios
ios/embed_test

# iOS port: Xcode derived data from the device build. project.yml is the source of
# truth; Spraak.xcodeproj is regenerated by xcodegen and already ignored (*.xcodeproj/).
/build/
